# This config is meant to be included from ~/.gitconfig
# Here's an example of how this can be done:
# git config --global include.path ~/.config/git/gitconfig
# git config --global include.path ~/.config/git/config.d/wsl --append
# git config --global include.path ~/.config/git/config.d/windows --append
# git config --global os.iswsl true
#
# Or manually add to ~/.gitconfig:
#[include]
#    path = ~/.config/git/gitconfig

[core]
    editor = nvim

[include]
    path = config.d/alias.gitconfig

# vim:ft=gitconfig:
